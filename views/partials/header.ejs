<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %> · MyShop</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link href="/css/style.css" rel="stylesheet" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/">MyShop</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav ms-auto">
            <% if (currentUser) { %>
              <li class="nav-item me-3 text-light d-flex align-items-center">
                Signed in as <strong class="ms-1"><%= currentUser.name %></strong>
                <% if (currentUser.rule === 'admin') { %>
                  <span class="badge bg-warning text-dark ms-2">admin</span>
                <% } %>
              </li>
              <li class="nav-item">
                <form method="post" action="/logout">
                  <button class="btn btn-outline-light btn-sm">Logout</button>
                </form>
              </li>
            <% } else { %>
              <li class="nav-item">
                <a class="btn btn-primary btn-sm" href="/login">Enter</a>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
    <main class="container my-4">

